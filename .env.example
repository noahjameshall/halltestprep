# Stripe — set this in Vercel (Project Settings -> Environment Variables) to enable real payments.
# Without it, the site runs in "demo mode" and unlocks courses instantly after checkout.
STRIPE_SECRET_KEY=sk_live_or_test_xxx
